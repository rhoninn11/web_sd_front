# a) Install dependencies
npm install react react-dom typescript websocket

# b) Run all necessary parts of the codebase
# Assuming that the WebSocket server is already running at ws://localhost:8000
# Assuming that the project was bootstrapped with Create React App or a similar tool that provides a start script
npm start
