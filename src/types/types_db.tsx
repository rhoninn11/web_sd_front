
// we need to replace this type with other actualy existing type

export interface Generation {
    db_id: number;
    title: string;
    promp: string;
    neg_prompt: string;
    b64_img: string;
}
